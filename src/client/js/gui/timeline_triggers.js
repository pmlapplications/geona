/**
 * Sets the triggers for events relating to the timebar.
 * @param {EventManager} eventManager The EventManager for the current map.
 * @param {JQuery}       geonaDiv    The div which contains the current map.
 */
export function registerTriggers(eventManager, geonaDiv, timeline) {
  // TODO write a full list of triggers which are in the Timeline itself


}
