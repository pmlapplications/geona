/**
   * All triggers for the timeline are set in the timeline methods themselves.
   * Below is a list of all the events that are triggered, and their methods:
   *
   * - 'timePanel.heightChanged'        - addTimelineLayer(), removeTimelineLayer()
   * - 'timePanel.setPikadayRange'      - addTimelineLayer(), removeTimelineLayer()
   * - 'timePanel.timelineChangeTime'   - triggerMapDateChange()
   * - 'timePanel.pikadayUpdateGraphic' - dragDate()
   */
